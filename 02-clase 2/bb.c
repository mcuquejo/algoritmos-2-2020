#include <stdio.h>

int main(void) {

    return 0;
}

int bb(int[] arreglo, int inicio, int fin){
    medio = len(arreglo) / 2
     
    int[] izq = arreglo[inicio, medio-1]
    
    int[] der = arreglo[medio, fin]
    return x;
}